{
  "name": "axios-kubernetes",
  "description": "Axios client with niceties for Kubernetes",
  "version": "0.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/possibilities/axios-kubernetes"
  },
  "author": "Mike Bannister",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/versal/possibilities/axios-kubernetes"
  },
  "main": "./index.js",
  "homepage": "https://github.com/possibilities/axios-kubernetes",
  "scripts": {
    "dev": "nodemon index.js",
    "dev:container": "npm-run-all --parallel dev:container:*",
    "dev:container:start": "nodemon --legacy-watch index.js",
    "dev:container:install": "nodemon --legacy-watch --exec 'yarn install' --watch yarn.lock",
    "start": "node index",
    "test": "echo no tests",
    "pretest": "standard"
  },
  "dependencies": {
    "JSONStream": "^1.3.1",
    "axios": "^0.17.1",
    "fs-extra": "^4.0.2"
  },
  "devDependencies": {
    "nodemon": "^1.12.1",
    "npm-run-all": "^4.1.2",
    "standard": "^10.0.3"
  }
}
